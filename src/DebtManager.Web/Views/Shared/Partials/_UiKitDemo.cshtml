@* Optional showcase snippet. Include via @await Html.PartialAsync("~/Views/Shared/Partials/_UiKitDemo.cshtml") when needed. *@
<section class="tw-card mt-6">
  <div class="tw-card-header">UI Kit Demo</div>
  <div class="tw-card-body space-y-4">
    <div class="flex flex-wrap items-center gap-2">
      <button class="tw-btn">Primary</button>
      <button class="tw-btn-outline">Outline</button>
      <button class="tw-btn-ghost">Ghost</button>
      <button class="tw-btn-danger">Danger</button>
      <button class="tw-btn-danger-outline">Danger Outline</button>
      <span class="tw-badge tw-badge-dev">DEV</span>
    </div>
    <div class="space-y-2">
      <div class="tw-alert-info">This is an info alert</div>
      <div class="tw-alert-success">Success! Your changes were saved.</div>
      <div class="tw-alert-warning">Heads up: this action is sensitive.</div>
      <div class="tw-alert-danger">Error: Something went wrong.</div>
    </div>
  </div>
</section>

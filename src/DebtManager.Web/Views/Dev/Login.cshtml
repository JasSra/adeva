@{
    Layout = "~/Views/Shared/_LandingLayout.cshtml";
    ViewData["Title"] = "Dev Login";
}

<div class="max-w-md mx-auto bg-white rounded-2xl shadow-2xl p-6 md:p-8 mt-20 backdrop-blur-sm/30">
    <h2 class="text-2xl font-semibold mb-3">Development Login</h2>
    <p class="text-sm text-gray-600 mb-4">Only available in Development environment.</p>

    <div class="mb-4 p-3 bg-blue-50 border border-blue-200 rounded text-sm">
        Prefer the new <a href="/Dev/FakeSignin" class="text-blue-700 underline">Fake Sign-in</a> to generate a test identity with optional Client/User scopes.
    </div>

    <form method="post">
        <div class="mb-4">
            <label class="block text-sm font-medium text-gray-700 mb-1">Username</label>
            <input name="username" class="w-full border rounded px-3 py-2" value="admin" />
        </div>
        <div class="mb-4">
            <label class="block text-sm font-medium text-gray-700 mb-1">Password</label>
            <input name="password" type="password" class="w-full border rounded px-3 py-2" value="admin" />
        </div>
    <button class="btn">Login</button>
    </form>
    @if (!ViewData.ModelState.IsValid)
    {
        <div class="mt-4 text-red-600 text-sm">Invalid credentials</div>
    }
</div>
